{
  "name": "cyclecare-app",
  "version": "1.0.0",
  "private": true,
  "description": "CycleCare web and Capacitor mobile frontend",
  "scripts": {
    "web": "python -m http.server 5500 --directory www",
    "cap:sync": "cap sync",
    "cap:open:android": "cap open android",
    "cap:run:android": "cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
